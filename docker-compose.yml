version: '3.8'

services:
  votafacil:
    image: votafacil_image
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
      - "443:443"
    networks:
      - votafacil_network

networks:
  votafacil_network:
    driver: bridge
