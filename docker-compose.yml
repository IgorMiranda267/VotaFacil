version: '3.8'

services:
  votafacil:
    image: votafacil_image
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=${DATABASE_URL}
    networks:
      - votafacil_network

networks:
  votafacil_network:
    driver: bridge
