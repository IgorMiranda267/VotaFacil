version: '3.8'

services:
  votafacil:
    image: votafacil_image
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "32714:8080"
    networks:
      - votafacil_network

networks:
  votafacil_network:
    driver: bridge
